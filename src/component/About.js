import React, {useContext} from 'react'
import noteContext from '../context/notes/NoteContext'

const About = () => {
    // const a = useContext(noteContext)
    // console.log(a)
  return (
    <div>
      {/* this is about {a.obj.name} studying in class {a.obj.class}. */}
    </div>
  )
}

export default About
